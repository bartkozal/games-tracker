# Games Tracker

## Development

1.  Install dependencies:

    ```sh
    $ yarn install
    ```

2.  Set up env variables:

    ```sh
    $ mv .env.example .env
    $ code .env
    ```

3.  Run the dev environment:

    ```sh
    $ yarn dev
    ```
