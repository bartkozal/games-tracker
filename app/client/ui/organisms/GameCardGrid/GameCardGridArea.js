import styled from "react-emotion";
import { SPACING_SMALL } from "ui/quarks";

const GameCardGridArea = styled("div")({
  display: "flex",
  flexWrap: "wrap",
  marginLeft: -SPACING_SMALL,
  marginRight: -SPACING_SMALL
});

export default GameCardGridArea;
