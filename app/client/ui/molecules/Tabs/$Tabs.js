import styled from "react-emotion";
import { SPACING_BASE } from "ui/quarks";

const $Tabs = styled("div")({
  display: "flex",
  justifyContent: "center",
  marginBottom: SPACING_BASE * 3
});

export default $Tabs;
