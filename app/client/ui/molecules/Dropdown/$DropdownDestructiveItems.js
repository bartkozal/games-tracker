import styled from "react-emotion";
import { SPACING_SMALL } from "ui/quarks";

const $DropdownDestructiveItems = styled("div")({
  marginTop: SPACING_SMALL
});

export default $DropdownDestructiveItems;
